#include <iostream>
using namespace std;

int main() 
{
int racineCaree(int a, double b) {
    while (a < 0)
    cout << "donner un nombre positif : ";
    cin >> a;
    if ( a > 0) {
        b = a * a
        cout << "sa racine carrée est : " b ;
    }
    else if
    { 
        cout << "svp positif";
    }
    else
    {
        break;
    }
return 0;
        }
    }

