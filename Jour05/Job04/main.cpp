#include <iostream>
#include <string>
#include "Joueur.hpp"
using namespace std;

int main()
{
    Joueur j1(0, 0);
    j1.afficher(0, 0, "");
    cout << j1.getx() << endl;
    // j1.setx(+1);
    // cout << j1.gety() << endl;
    // j1.sety(+1);
    // j1.deplacer(x, y);
    // j1.afficher(1, 1);
    Joueur j2(0,0);
    j2.afficher(0,0, "");
    cout << j1.getx() << endl;
    // j2.setx(+1);
    // cout << j2.gety() << endl;
    // j2.sety(+1);
    // j2.deplacer(x, y);
    // j2.afficher(1, 1);
    Joueur j3(0,0);
    j3.afficher(0,0, "");
    // j3.setx(+1);
    // cout << j3.gety() << endl;
    // j3.sety(+1);
    // j3.deplacer(x, y);
    // j3.afficher(1, 1);
    Joueur j4(0,0);
    j4.afficher(0,0, "");
    // j4.setx(+1);
    // cout << j4.gety() << endl;
    // j4.sety(+1);
    // j4.deplacer(x, y);
    // j4.afficher(1, 1);

return 0;
}